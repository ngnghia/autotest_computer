pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "build process successfully done..."
                //bat 'start cmd.exe /c C:\\Users\\DELL\\cucsuc.bat'
            }

        }
    }
}