pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "build done ..."
                bat 'start cmd.exe /c C:\\Users\\DELL\\cucsuc.bat'
                //bat 'start cmd.exe'
            }

        }
    }
}